<p>Bienvenido <%= current_user.name%>!</p>

<p>Puedes confirmar tu cuenta ingresando al siguiente link:</p>

<p><%= link_to 'Confirmar mi cuenta', confirmation_url(@resource, :confirmation_token => @token) %></p>
